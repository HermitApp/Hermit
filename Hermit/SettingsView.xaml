<Controls:MetroWindow x:Class="Hermit.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding DisplayName}" Height="500" Width="500" EnableDWMDropShadow="True" WindowStartupLocation="CenterOwner">
    <Grid>
        <Controls:MetroTabControl x:Name="PluginSettings">
            <Controls:MetroTabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}"/>
                </DataTemplate>
            </Controls:MetroTabControl.ItemTemplate>
        </Controls:MetroTabControl>
    </Grid>
</Controls:MetroWindow>
